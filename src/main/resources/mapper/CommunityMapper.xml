<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="org.ykk.jobbridge.mapper.ICommunityMapper">


    <select id="getPostList" resultType="CommunityPostDTO">
        SELECT P.id, P.member_id, P.category, P.title, M.name AS author, P.content,
               (SELECT COUNT(1) FROM community_comment C
                 WHERE C.post_id = P.id AND C.status = 'ACTIVE') AS replies,
               P.view_count AS views, P.view_count, P.like_count, P.status,
               P.created_at, P.updated_at
          FROM community_post P, member M
         WHERE P.member_id = M.id
           AND P.status = 'ACTIVE'
         ORDER BY P.created_at DESC, P.id DESC
    </select>

    <select id="getPostInfo" parameterType="CommunityPostDTO" resultType="CommunityPostDTO">
        SELECT P.id, P.member_id, P.category, P.title, M.name AS author, P.content,
               (SELECT COUNT(1) FROM community_comment C
                 WHERE C.post_id = P.id AND C.status = 'ACTIVE') AS replies,
               P.view_count AS views, P.view_count, P.like_count, P.status,
               P.created_at, P.updated_at
          FROM community_post P, member M
         WHERE P.member_id = M.id
           AND P.id = #{id}
    </select>


    <insert id="insertPostInfo" parameterType="CommunityPostDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO community_post
            (
                member_id, category, title, content, status
            )
        VALUES (
            #{memberId}, #{category}, #{title}, #{content}, 'ACTIVE'
        )
    </insert>


    <update id="updatePostViews" parameterType="CommunityPostDTO">
        UPDATE community_post SET
            view_count = view_count + 1
        WHERE
            id = #{id}
            AND status = 'ACTIVE'
    </update>


    <update id="deletePostInfo" parameterType="CommunityPostDTO">
        UPDATE community_post SET
            status = 'DELETED',
            updated_at = CURRENT_TIMESTAMP
        WHERE
            id = #{id}
            AND member_id = #{memberId}
            AND status = 'ACTIVE'
    </update>

    <select id="getCommentList" parameterType="CommunityCommentDTO" resultType="CommunityCommentDTO">
        SELECT C.id, C.post_id, C.member_id, M.name AS author, C.content,
               C.status, C.created_at, C.updated_at
          FROM community_comment C, member M
         WHERE C.member_id = M.id
           AND C.post_id = #{postId}
           AND C.status = 'ACTIVE'
         ORDER BY C.created_at, C.id
    </select>

    <insert id="insertCommentInfo" parameterType="CommunityCommentDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO community_comment
            (
                post_id, member_id, content, status
            )
        VALUES (
            #{postId}, #{memberId}, #{content}, 'ACTIVE'
        )
    </insert>

    <update id="deleteCommentInfo" parameterType="CommunityCommentDTO">
        UPDATE community_comment SET
            status = 'DELETED',
            updated_at = CURRENT_TIMESTAMP
        WHERE
            id = #{id}
            AND member_id = #{memberId}
            AND status = 'ACTIVE'
    </update>

    <select id="getReportList" parameterType="CommunityReportDTO" resultType="CommunityReportDTO">
        SELECT id, post_id, reporter_member_id, reason, detail, status, created_at
          FROM community_report
         WHERE post_id = #{postId}
           AND reporter_member_id = #{reporterMemberId}
    </select>

    <select id="getReportExists" parameterType="CommunityReportDTO" resultType="CommunityReportDTO">
        SELECT CASE WHEN COUNT(1) > 0 THEN 'Y' ELSE 'N' END AS EXISTS_YN
          FROM community_report
         WHERE post_id = #{postId}
           AND reporter_member_id = #{reporterMemberId}
    </select>

    <insert id="insertReportInfo" parameterType="CommunityReportDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO community_report
            (
                post_id, reporter_member_id, reason, detail, status
            )
        VALUES (
            #{postId}, #{reporterMemberId}, #{reason}, #{detail}, 'PENDING'
        )
    </insert>

</mapper>
