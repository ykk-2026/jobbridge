<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- JAVA와 연결할 Mapper 파일 설정 -->
<mapper namespace="org.ykk.jobbridge.mapper.IMemberMapper">

    <!-- 회원 아이디 중복 체크 쿼리 : COUNT 함수 사용 시 결과는 반드시 레코드가 존재함 -->
    <select id="getLoginIdExists" parameterType="MemberDTO" resultType="MemberDTO">
        SELECT CASE WHEN COUNT(1) > 0 THEN 'Y' ELSE 'N' END AS EXISTS_YN
          FROM member
         WHERE login_id = #{loginId}
    </select>

    <!-- 이메일 중복 체크 쿼리 -->
    <select id="getEmailExists" parameterType="MemberDTO" resultType="MemberDTO">
        SELECT CASE WHEN COUNT(1) > 0 THEN 'Y' ELSE 'N' END AS EXISTS_YN
          FROM member
         WHERE email = #{email}
    </select>

    <!-- 회원 가입 쿼리 : id 컬럼은 AUTO_INCREMENT이며, 생성된 값은 pDTO의 id 변수에 저장됨 -->
    <insert id="insertMemberInfo" parameterType="MemberDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO member
            (
                login_id, password, name, birth_date, gender, email, phone, role, status
            )
        VALUES (
            #{loginId}, #{password}, #{name}, NULLIF(#{birthDate}, ''), NULLIF(#{gender}, ''),
            #{email}, #{phone}, #{role}, 'ACTIVE'
        )
    </insert>

    <!-- 구직자 프로필 기본 정보 등록 쿼리 -->
    <insert id="insertJobSeekerProfile" parameterType="MemberDTO">
        INSERT INTO job_seeker_profile
            (
                member_id, desired_job
            )
        VALUES (
            #{id}, NULLIF(#{desiredJob}, '')
        )
    </insert>

    <!-- 로그인 쿼리 : 아이디와 암호화된 비밀번호가 일치하는 회원 조회 -->
    <select id="getLogin" parameterType="MemberDTO" resultType="MemberDTO">
        SELECT id, login_id, name, role, status
          FROM member
         WHERE login_id = #{loginId}
           AND password = #{password}
           AND status = 'ACTIVE'
    </select>

</mapper>
